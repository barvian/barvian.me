---
import { getVideo } from '@/utils/assets'
import type { ProjectEntry } from '@/schemas/work'
import ProjectVideoVue from './ProjectVideo.vue'

type Props = {
	entry: ProjectEntry
}

const { entry } = Astro.props
const { slug } = entry

const { poster, src } = await getVideo(entry)
---

<ProjectVideoVue {slug} {poster} {src} client:load transition:persist={`${slug}-hero`} />
